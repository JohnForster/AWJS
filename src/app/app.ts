import Engine from '../gameEngine/engine'
import { uiContexts } from '../gameEngine/model/uiModel/IUIState';

export default new class App {
  public run() { 
    const engine = new Engine()
  }
} ()
